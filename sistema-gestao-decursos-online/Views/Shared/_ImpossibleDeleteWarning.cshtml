@model dynamic //recebe um modelo dinâmico com as propriedades das classes Usuario, Consulta e Controller

<div class="alert alert-warning border-warning">
    <h4 class="alert-heading">
        <i class="bi bi-exclamation-diamond"></i> Exclusão Bloqueada
    </h4>
    <p>
        Não é possível excluir este(a) <strong>@Model.Nome</strong> porque ele(a) está vinculado(a) a <strong>@Model.QtdConsultas</strong> registro(s) relacionado(s).
    </p>
    <hr />
    <p class="mb-0">
        Por favor, remova ou atualize esses registros associados antes de tentar novamente.
    </p>
    <a asp-controller="@Model.Controller" asp-action="Index" class="btn btn-warning mt-3">
        <i class="bi bi-arrow-left-circle"></i> Retornar à lista
    </a>
</div>